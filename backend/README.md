# Бэкенд SiYuan Share

Бэкенд-сервис для плагина публикации заметок SiYuan, включающий API и веб-интерфейс.

## Структура проекта

```
backend/
├── api/                 # API на Go
│   ├── main.go
│   ├── models/         # Модели данных
│   ├── controllers/    # Контроллеры
│   ├── middleware/     # Промежуточное ПО
│   ├── routes/         # Маршруты
│   └── README.md
└── web/                # Фронтенд на React
    ├── src/
    ├── public/
    └── README.md
```

## Технологический стек

### API (Backend)
- **Go 1.24**
- **Gin** - веб-фреймворк
- **GORM** - ORM
- **SQLite** - база данных (github.com/glebarez/sqlite)

### Фронтенд (Web)
- **React 18**
- **TypeScript**
- **Vite**
- **React Router**
- **Axios**
- **React Markdown**

## Быстрый старт

> 💡 Рекомендуется использовать [Task](https://taskfile.dev/) для запуска задач проекта.

### Использование Task (рекомендуется)

Установите [Task](https://taskfile.dev/):
```bash
# Linux
sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin
```

Основные команды:
```bash
# Установка всех зависимостей
task install

# Запуск API в режиме разработки
task api:dev

# Запуск фронтенда в режиме разработки (в другом терминале)
task web:dev

# Сборка приложения (фронтенд + бэкенд в одном бинарном файле)
task build

# Запуск собранного приложения
task run

# Список всех доступных задач
task --list
```

### Ручной запуск

#### API (Backend)

```bash
cd api
go mod download
go run main.go
```

Порт по умолчанию: 8088

#### Фронтенд (Web)

```bash
cd web
npm install
npm run dev
```

Порт по умолчанию: 5173 (Vite)

## Переменные окружения

### API сервис

- `PORT` - порт сервиса (по умолчанию: 8088)
- `DATA_DIR` - директория данных (по умолчанию: ./data)
- `GIN_MODE` - режим Gin (release/debug)

### Фронтенд (Web)

Создайте файл `web/.env.local`:

```
VITE_API_URL=http://localhost:8088
```

## Документация API

Подробности см. в [api/README.md](./api/README.md)

## Деплой (Развертывание)

### Среда разработки

1. Запустите API:
```bash
cd api && go run main.go
```

2. Запустите сервер разработки фронтенда:
```bash
cd web && npm run dev
```

### Продуктовая среда (встроенный фронтенд)

Бэкенд интегрирует статические файлы фронтенда, поэтому достаточно собрать один исполняемый файл:

**Используя Task (рекомендуется):**
```bash
# Сборка одной командой
task build

# Запуск
cd api
./siyuan-share-api
```

**Ручная сборка:**
```bash
# 1. Сборка фронтенда
cd web
npm install
npm run build

# 2. Сборка бэкенда (автоматически встроит содержимое web/dist)
cd ../api
go build -o siyuan-share-api

# 3. Запуск
./siyuan-share-api  # Доступ по адресу http://localhost:8088
```

**Важно**:
- Файлы фронтенда встраиваются в бинарный файл с помощью Go `embed`.
- Перед сборкой бэкенда обязательно соберите фронтенд (директория `web/dist` должна существовать).
- Фронтенд автоматически использует относительные пути для доступа к API.

## База данных

Используется SQLite, файл базы данных находится в `api/data/siyuan-share.db`.

### Структура таблиц

#### Таблица `shares`
- Записи о публикациях.
- Содержит контент документа, пароли, время истечения и т.д.

#### Таблица `users`
- Информация о пользователях.
- Авторизация через API Token.

Подробности см. в: [api/models/](./api/models/)

## Разработка

### Инициализация базы данных

При первом запуске API структура таблиц будет создана автоматически.

### Создание пользователя

Используйте утилиту:
```bash
cd api
go run tools/create_user.go -username user -email email@example.com
```

## Лицензия

MIT
